apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml

commonLabels:
  app.kubernetes.io/name: insight-gallery
  app.kubernetes.io/part-of: openms-webapps
